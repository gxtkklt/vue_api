
server:
  port: 8081


#cxf:
#  path: /aaa

spring:

  #数据库连接
  datasource:
    #com.mysql.cj.jdbc.Driver 需要指定时区serverTimezone;
    url: jdbc:mysql://127.0.0.1:3307/shop?serverTimezone=Hongkong&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: ts
    #com.mysql.cj.jdbc.Driver  可能是jdbc 版本号不一样  不清楚
    driver-class-name: com.mysql.jdbc.Driver
    # 数据库连接池的核心类
    #type: com.alibaba.druid.pool.DruidDataSource





mybatis:
  # 注意：对应mapper映射xml文件的所在路径
  mapper-locations: classpath:xml/*.xml
  # 注意：对应实体类的路径
  type-aliases-package: cn.ts.po




